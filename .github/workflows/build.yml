name: Deploy to Web Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Deploy HTML File
      env:
        WEB_SERVER_TOKEN: ${{ secrets.WEB_SERVER_TOKEN }}
      run: |
        # Replace the following commands with your actual deployment commands
        echo "Deploying HTML file"
        echo "Using secret: $WEB_SERVER_TOKEN"
        
        # Replace the placeholder in the HTML file with the secret value
        sed -i "s|<API_TOKEN_PLACEHOLDER>|$WEB_SERVER_TOKEN|g" index.html
        
        # Your deployment commands here
